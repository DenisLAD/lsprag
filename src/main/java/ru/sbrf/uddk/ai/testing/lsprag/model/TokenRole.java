package ru.sbrf.uddk.ai.testing.lsprag.model;

public enum TokenRole {
    CONDITION,      // Участвует в условном выражении
    METHOD_CALL,    // Вызов метода
    VARIABLE        // Ссылка на переменную
}
